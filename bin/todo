#!/usr/bin/env php
<?php
require __DIR__ . '/../vendor/autoload.php';

use Pitan76\Todofile\Main;

$main = new Main($argv);

try {
    exit($main->run());
} catch (Exception $e) {
    echo $e->getMessage();
    exit(1);
}
